# Copy to .env.local for overrides. .env.local is gitignored by Vite by default.

# Base URL for the backend REST API. Default matches the Django dev server.
VITE_API_BASE_URL=http://localhost:8000/api
